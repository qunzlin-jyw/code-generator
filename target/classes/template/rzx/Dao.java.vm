package ${daoPath};

import ${entityPath}.${className};
import java.util.List;

/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Dao{

    List<${className}> query($className form);

    int count($className form);

    int update($className form);

    int save($className form);

    $className getDetail(Long id);

    int delete(Long id);
	
}
